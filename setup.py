from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a ml project to predict emotions from text',
    author='campusx',
    license='',
)
